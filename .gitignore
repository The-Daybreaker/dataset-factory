# === workspace 仓的 .gitignore：可再生产物（能由 uv / pytest 重新生成）不入库 ===

# Python 字节码 / 缓存
__pycache__/
*.py[cod]

# uv 虚拟环境（可再生：uv sync 重建）
.venv/

# 测试 / 覆盖率缓存与报告
.pytest_cache/
.coverage
.coverage.*
htmlcov/

# 打包 / 构建产物
dist/
build/
*.egg-info/

# 注意：uv.lock 要入库（锁定确切版本、保证可复现安装），不要忽略它
