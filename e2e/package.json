{
  "name": "dataset-factory-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Dataset Factory 跨前后端浏览器 E2E（Playwright）：真实浏览器 → 真实后端 → 假模型端点",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
