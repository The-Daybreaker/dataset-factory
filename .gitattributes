# 统一换行符：文本文件在仓库里一律存 LF、检出也按 LF，避免 Windows/Linux 之间 CRLF 抖动
# （CI 跨 Win+Linux 跑，统一 LF 保证一致；Windows 上 git 不再把 LF 转成 CRLF）
* text=auto eol=lf

# 二进制类型：不做任何换行转换（将来素材 / 图片 / 字体进仓时保护它们不被破坏）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary

# 例外：Windows 批处理必须 CRLF——cmd 对 LF 的多行括号块解析不可靠（后行规则覆盖上面的全 LF）
*.bat text eol=crlf
*.cmd text eol=crlf
