# 更新日志

本项目的所有对外可见变更按版本记录于此。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 0.2.0 — 二期「打标闭环」（功能已全部合入 main，版本号与标签待发布时定）

把一期的「单张对话打标」升级为批量投产：素材进工作目录 → 批量打标 → 同名 txt 产物 → 打包 zip 交付训练。

- **工作目录**：一个目录可存多个批次；素材支持「复制导入」（工具把文件复制进目录并记账）与「就地采用」（指认已有目录、不复制）两种来路；目录可搬迁路径、可删除（先给影响面预览与确认），配套完整性对账（缺素材 / 缺产物 / 孤儿文件逐条列出并给出处置入口）。
- **批量打标**：图片与视频混批同场投产；逐条实时上屏进度与状态；失败可按原因分类选择性重打、维护重试列表（加入 / 移出 / 清空 / 只打这批）；运行中可停止，产物按已写出的保留。
- **运行日志与出身**：每场跑批留日志与条目清单，弹窗按分段看（含聚合表）；批次顶栏显示运行状态章与「已完成 / 总数」进度。
- **策略库跨目录复用**：把「端点 + 基础提示词 + 启用的 Skill」这套组合存成库策略，跨工作目录反复使用；被引用的策略删除前先给出引用清单；每场跑批固化一份配置快照（含哈希），产物可追溯到当时的真实配置。
- **打包导出**：按批次出计划预览（入包 / 被排除、字节数、顺序重命名开关），确认后打 zip 下载交付训练。
- **CLI 与 Web 同口径**：`dsf workdir / batch / export / strategy` 四组命令覆盖同一批能力，输出结构化、退出码语义固定，可与界面混用。
- **界面与体验**：按原型逐屏对齐（按钮档位与图标尺寸、行内入口归位、提示气泡、设置页字号与间距刻度、日志弹窗重构、打标页信息补齐），亮 / 暗双主题与窄屏布局同步校平。

## 0.1.0 — 2026-09-15

首个版本，交付 **AI 打标核心能力**（一期）。

- **打标核心**：发图片或视频 + 指令产出 caption，回复流式呈现（思考型模型的思考过程单独展示、可展开回看）；多轮迭代改写，会话自动落盘、重启可恢复。
- **双入口**：Web 端（提示词工作台 + 设置容器，亮 / 暗双主题）与 CLI（`dsf`），能力对等；CLI 固定退出码语义、`--json` 结构化输出，供外部 agent 可靠调用。
- **提示词库**：结构化管理（增删改查、改名、旧版滚动备份），每轮打标必选一条基础提示词。
- **Skill 扩展**：导入 agentskills.io 标准 skill 包，打标时可选启用（SKILL.md 与 references/ 全文注入）；包内文件只读预览。
- **端点管理**：OpenAI 兼容端点多套配置并存与切换、连通性测试、高级请求参数（temperature / top_p / max_tokens / extra_body / 超时 / 重试）随配置存取；密钥文件（Unix 0600）与环境变量双通道。
- **服务生命周期**：一键脚本无窗口启动 / 幂等启动（校验监听进程身份，防端口误判与误杀）、界面或脚本关闭、运行日志落盘可查看。
- **可观测性与工程质量**：分层日志 + 请求 id 贯穿、请求信封先落盘可复盘；OpenAPI 契约为单一事实源（前端类型自动生成、CI 卡破坏性变更）；CI 全门禁（测试 / 覆盖率 / 架构契约 / 严格类型 / lint / 密钥扫描，Windows + Linux 双矩阵）。
