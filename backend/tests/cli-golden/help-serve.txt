退出码：0
---- 输出 ----

 Usage: root serve [OPTIONS]

 启动本地 Web 服务（HTTP API + 前端界面）。

 前台运行 Ctrl+C 停止；无窗口后台运行（start 脚本默认）用界面电源按钮或 stop
 脚本。

┌─ Options ───────────────────────────────────────────────────────────────────┐
│ --host             <str>  监听地址 [default: 127.0.0.1]                     │
│ --port             <int>  监听端口 [default: 8000]                          │
│ --log-level        <str>  日志级别（DEBUG / INFO / WARNING /                │
│                           ERROR）；缺省读环境变量 DSF_LOG_LEVEL，再缺省     │
│                           INFO                                              │
│ --help                    Show this message and exit.                       │
└─────────────────────────────────────────────────────────────────────────────┘
