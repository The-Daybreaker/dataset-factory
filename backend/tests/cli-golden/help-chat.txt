退出码：0
---- 输出 ----

 Usage: root chat [OPTIONS]

 终端多轮打标：交互输入指令（附图 / 附视频用 `@文件路径 指令`），Ctrl+D /
 Ctrl+C 退出。

┌─ Options ───────────────────────────────────────────────────────────────────┐
│ --prompt            -p      <str>                   基础提示词名称；新会话… │
│ --skill             -s      <str>                   启用的                  │
│                                                     skill（可多次）；首轮 … │
│ --video-fps                 <int range> [1<=x<=10]  @ 附带视频的抽帧        │
│                                                     fps（整型 1–10，默认    │
│                                                     2）                     │
│                                                     [default: 2]            │
│ --video-max-frames          <int>                   @                       │
│                                                     附带视频的抽帧帧数上限… │
│                                                     16）                    │
│                                                     [default: 16]           │
│ --session                   <str>                   要恢复的会话            │
│                                                     id；缺省自动取最新会话  │
│ --help                                              Show this message and   │
│                                                     exit.                   │
└─────────────────────────────────────────────────────────────────────────────┘
