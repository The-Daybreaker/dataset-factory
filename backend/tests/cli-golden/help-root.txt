退出码：0
---- 输出 ----

 Usage: root [OPTIONS] COMMAND [ARGS]...

 Dataset Factory —— AI 打标工具（发图 + 指令产出 caption，支持迭代改写）。

┌─ Options ────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ --help          Show this message and exit.                                                                          │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
┌─ Commands ───────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ label     单发打标：发图片或视频 + 指令，输出 caption；带 --session 续接即迭代改写。                                 │
│ chat      终端多轮打标：交互输入指令（附图 / 附视频用 `@文件路径 指令`），Ctrl+D / Ctrl+C 退出。                     │
│ run       前台执行批次，Ctrl-C 等待当前条目安全收尾并输出中断报告。                                                  │
│ serve     启动本地 Web 服务（HTTP API + 前端界面）。                                                                 │
│ config    端点配置管理（base_url / 模型名 / API 密钥，支持多套配置）                                                 │
│ prompt    提示词库管理（增删改查）                                                                                   │
│ skill     Skill 包管理（agentskills.io 标准）                                                                        │
│ session   会话查看（list / show）                                                                                    │
│ workdir   工作目录管理                                                                                               │
│ batch     批次管理                                                                                                   │
│ export    数据集导出                                                                                                 │
│ strategy  策略库管理                                                                                                 │
└──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

 退出码：0 成功；1 运行失败或用户中止；2 命令用法错误（参数缺失/拼错、非交互缺 --yes）；130 被 Ctrl-C 打断。
